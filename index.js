// const testVar = {}

// function testFunc() {
//   return "hi"
// }

const superbowlWin = (records) => records.find(record => record.result === "W") ? records.find(record => record.result === "W").year : undefined
